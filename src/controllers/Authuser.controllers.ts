import express from 'express';
import { Extradata, ExtradataLogin } from '../config/interfaces/Auth.interfaces';
import AuthServices from '../services/Auth.services';
import Util from '../utils/Utils';

export default class AuthUserControllers {
  static async createnewuser(req: express.Request, res: express.Response) {
  }
}
